import type { Dict } from "../types";

export interface SocialProofCopy {
  label: string;
}

export const socialProofCopy: Dict<SocialProofCopy> = {
  es: { label: "Integrado con las plataformas que ya usas" },
  en: { label: "Integrated with the platforms you already use" },
};
